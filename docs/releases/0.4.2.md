# pi-web 0.4.2

0.4.2 improves mobile keyboard behavior on Android Chrome.

## Fixes

- Add `interactive-widget=resizes-content` to the viewport meta tag so supported mobile browsers resize the content viewport when the virtual keyboard appears, reducing phantom empty space around the composer.
