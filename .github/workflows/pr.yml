name: PR Checks

on:
  pull_request:
    branches:
      - "**"

jobs:
  test:
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: 24
          package-manager-cache: false

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install chromium

      # The hosted macOS runner cannot reliably execute Playwright's emulated
      # touch projects; those remain covered by the full local sharded suite.
      - name: Full desktop and auth test suite
        run: PI_WEB_E2E_PROJECTS=desktop,auth PI_WEB_E2E_SHARDS=3 npm test

      - name: Upload Playwright report
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: playwright-report
          path: |
            playwright-report/
            test-results/
          retention-days: 7
